Insert Timestamp _
	
%%Python
class InsertTimestampCommand:

	name = 'Insert Timestamp'
	inline_safe = True
	
	def run(self):
		self.project_list.current_project.editor_insert_timestamp()

ThisProject.add_action(InsertTimestampCommand)
%%
