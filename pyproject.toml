[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "urtext"
version = "0.1.1"
description = "Programmable knowledge base."
readme = "readme.md"