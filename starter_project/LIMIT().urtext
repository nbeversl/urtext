LIMIT() _ #call

Limits INCLUDE to the specified number of results.

Example: 
	[[  >(| Example Use of LIMIT() >) +(*) LIMIT(10) LIST() ]]
	Includes all nodes in the project, up to a limit of 10.

Output:

{~ Example Use of LIMIT() _
| About Urtext >
| Action Links ^ Links >
| Action Links ^ WHEN() >
| Arguments >
| Asterisk ^ INCLUDE() >
| Auto-Completions >
| Bold Text >
| Bracket Node Wrappers >
| Bracket Nodes >
| Bug list >
}

See next: | SORT() >